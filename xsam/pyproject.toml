[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "xsam"
version = "0.0.0"
description = "X-SAM core package"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["xsam*"]
